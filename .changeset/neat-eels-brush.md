---
"@prismforge/token-cli": minor
---

Add an interactive `prismforge init` wizard that asks for provider, repository, token targets, and optional Token Studio inclusion. Extend non-interactive init flags with `--targets` and `--studio`, and support tokens-only workspace scaffolds for consumers who do not want the Studio UI.

