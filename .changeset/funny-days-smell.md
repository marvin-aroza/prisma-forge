---
"@prismforge/token-cli": minor
---

Improve `prismforge init` for external consumers with an interactive setup flow (targets + optional Token Studio) and tokens-only scaffold support. Also fix CLI startup module resolution by lazy-loading runtime token modules so `npx @prismforge/token-cli` works outside the monorepo.

