# Changesets

Use changesets to define release intent.

- Stable release: regular changeset.
- Next release: use pre mode and publish with `--tag next`.

Commands:

```bash
pnpm changeset
pnpm changeset version
pnpm changeset publish
```

